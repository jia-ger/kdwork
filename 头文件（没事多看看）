#include<stdio.h>==#include<cstdio>//printf scanf函数
#include<algorithm>//可以使用max,min,sort函数  std::max() sort函数默认升序排序
using namespace std;//可以直接用max不用输std::
#include <iostream>//cin cout函数不过也得是std::cin>>a>>b;std::cout<<a<<b;
#include<cmath>//可以使用pow函数pow（a,b）即a的b次方
sqrt()//用于计算平方根   cbrt（）//用于计算立方根
万能头文件#include<bits/stdc++.h>  //非常好用，只会影响编译时间，不会影响运行时间
字符串的每一个下标对应一个字符
bool cmp(int a, int b) {
    return a > b;
}
这个函数表示我们希望把数组中的元素从大到小排序。
每当sort函数需要比较两个元素的大小时，它会调用cmp函数，根据返回值的大小来确定元素排序的顺序
sort(a, a+10,cmp)调用cmp从大到小排序
https://blog.csdn.net/weixin_45031801/article/details/137522007    //unique<去重函数> 返回的是去重后最后一个不重复元素的地址
