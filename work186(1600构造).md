https://codeforces.com/problemset/problem/1899/F

```c++
#include <bits/stdc++.h>
using namespace std;
#define itn int
#define icn cin
#define int long long
#define endl '\n'
typedef pair<int,int> PII;
const int N=1e4+10;
const int INF=1e18;
const int mod=1e9+7;
#define ll __int128


int n,q; 
void solve()
{
	cin >> n >> q; int p = n - 1;
    for (int i = 1; i < n; ++i) cout << i << ' ' << i + 1 << "\n";//连成一条链 
    for (int i = 1, d; i <= q; ++i) {//分成1-2-3-..-p 和 p-n 
        cin >> d;
        if(p == d) cout << "-1 -1 -1\n";
        else cout << n << ' ' << p << ' ' << d << "\n", p = d;
    }
	 
}


signed main() {
	cin.tie(0)->sync_with_stdio(0);
	int T=1;
	cin>>T;

	while(T--)solve();

	return 0;
}
```
