https://atcoder.jp/contests/abc422/tasks/abc422_e
分析：假设有一条 ax+by+c=0 线穿过一半以上的线段。那么，这条直线上至少有 （N+1）/2个点。因此，如果从 
N 个点中随机选择两个，那么这两个点都位于这条直线上的概率（n+1）/2/n*[((n+1)/2-1)/(n-1)]=(n+1)/4n>1/4
换句话说， p1和 p2都在这条直线上的概率为 1/4 或更高。可以直接随机数取两点当作答案要求的直线O(n)循环一下看有多少点在这条直线上

随机数：// 最常用的引擎
std::mt19937 mt;                    // 32位梅森旋转
std::mt19937_64 mt64;               // 64位梅森旋转
（58）是种子，确保多次运行的随机数结果一致

#include <iostream>
#include <random>
using namespace std;
using ll = long long;

ll N, X[500500], Y[500500];

int main() {
  cin >> N;
  for (int i = 0; i < N; i++) cin >> X[i] >> Y[i];

  mt19937_64 rng(58);

  int T = 100;
  for (int t = 0; t < T; t++) {
    int i, j;
    do {
      i = rng() % N, j = rng() % N;
    } while (i == j);
    ll a = Y[i] - Y[j];
    ll b = X[j] - X[i];
    ll c = X[i] * Y[j] - X[j] * Y[i];
    int num = 0;
    for (int k = 0; k < N; k++) num += a * X[k] + b * Y[k] + c == 0;
    if (num * 2 > N) {
      cout << "Yes\n";
      cout << a << " " << b << " " << c << "\n";
      return 0;
    }
  }
  cout << "No\n";
}
